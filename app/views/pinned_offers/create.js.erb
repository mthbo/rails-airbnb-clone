<% unless @pinned_offer.errors.any? %>
  var $pin_box = $('[data-pin-offer-id=<%= @offer.id %>]')
  $pin_box.hide();
  $pin_box.empty();
  var $unpin_button = $("<%= j render 'pinned_offers/pin_button', offer: @offer %>");
  $pin_box.append($unpin_button);
  $pin_box.slideDown('fast');
<% end %>
