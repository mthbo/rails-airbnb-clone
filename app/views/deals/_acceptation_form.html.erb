<%= simple_form_for [@offer, @deal] do |f| %>
  <%= f.error_notification message: 'Please fill all the fields below' %>
  <%= f.input :accepted_at, as: :hidden, :input_html => { value: DateTime.now } %>
  <%= f.button :submit, "Accept the proposition", class: "btn btn-primary" %>
<% end %>
