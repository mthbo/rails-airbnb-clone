<div class="form-separation padded-top-xs padded-bottom-s">
  <div></div>
  <p class="font-weight-normal">Session cancellation</p>
  <div></div>
</div>

<% if @deal.advisor == current_user %>
  <p>Cancel the session if you don't want to pursue. Note that you won't be able to cancel the session after a proposition is sent, unless <%= @deal.client.first_name %> declines it.</p>
<% elsif @deal.client == current_user %>
  <p>Cancel the session if you don't want to pursue. Note that you won't be able to cancel the session once you accept a proposition.</p>
<% end %>

<div class="text-center padded-top-xs">
  <button type="button" class="btn btn-form btn-red" data-toggle="modal" data-target="#session-cancellation">
    Cancel the session
  </button>
</div>


<div class="modal fade" id="session-cancellation" tabindex="-1" role="dialog" aria-labelledby="sessionCancellationLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title font-weight-normal" id="sessionCancellationLabel">Session cancellation</h4>
      </div>
      <div class="modal-body">
        <p>You won't be able to do anything else on this session after cancellation.</p>
        <p>Are you sure you want to do this?</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-form btn-gray" data-dismiss="modal">Go back</button>
        <%= link_to "Cancel the session", deal_path(@deal, deal:{status: :cancelled}), method: :patch, class: "btn btn-form btn-red" %>
      </div>
    </div>
  </div>
</div>




