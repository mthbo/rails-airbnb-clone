<div class="modal fade" id="proposition-confirmation" tabindex="-1" role="dialog" aria-labelledby="propositionConfirmationLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="propositionConfirmationLabel">Proposition submission</h4>
      </div>
      <div class="modal-body">
        <p class="font-weight-normal blue">Review your proposition before confirming submission.</p>
        <p>After submission, remind that you are commited to deliver the service you are proposing, which is still subject to acceptation by <%= deal.client.first_name %>.</p>
        <div class="form-separation padded-top-s padded-bottom-xs">
          <div></div>
          <p>
            <strong>Proposition expiring on <%= deal.proposition_deadline.strftime("%d %b %Y") if deal.proposition_deadline.present? %></strong>
          </p>
          <div></div>
        </div>
        <%= render 'proposition_recap', deal: deal %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-form btn-gray" data-dismiss="modal">Go back</button>
        <%= link_to submit_proposition_deal_path(deal), method: :patch, class: "btn btn-form btn-green" do %>
          <i class='fa fa-file-text-o fa-lg' aria-hidden='true'></i>&nbsp;&nbsp; Submit
        <% end %>
      </div>
    </div>
  </div>
</div>
