$('#offer_reviews_page').html("<%= j render 'reviews' %>");
$('#offer-reviews-paginator').html("<%= j paginate @deals_reviewed, remote: true %>");
window.scroll({ top: eval($("#index-wrapper").offset().top - 300), left: 0, behavior: 'smooth' });
