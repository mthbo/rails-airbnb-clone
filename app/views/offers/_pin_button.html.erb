<%= link_to pin_offer_path(offer), method: :post, remote: :true, 'data-pin-offer-id': offer.id, class: current_user.liked?(offer) ? 'unpin-offer-button' : 'pin-offer-button' do %>
  <i class="fa fa-thumb-tack" aria-hidden="true"></i>
<% end %>
