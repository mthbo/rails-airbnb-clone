<p>
  <span class="info-icon"><%= image_tag 'amount.svg' %></span>
  <% if offer.free? %>
    <strong>FREE</strong>
  <% elsif offer.median_amount_money.nil? %>
    <span class="medium-gray">No price yet</span>
  <% else %>
    <span class="medium-gray"><%= money_without_cents_and_with_symbol(offer.min_amount_money) %> &mdash; </span>
    <strong><%= money_without_cents_and_with_symbol(offer.median_amount_money) %></strong>
    <span class="medium-gray"> &mdash; <%= money_without_cents_and_with_symbol(offer.max_amount_money) %></span>
  <% end %>
</p>
