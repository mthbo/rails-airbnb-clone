<% content_for :title do %>
  <%= "Papoters | Edit offer" %>
<% end %>

<h2>Edit offer</h2>

<div data-offer-status-id="<%= @offer.id %>" class="offer-trigger-slider">
  <%= render 'offers/status_trigger', offer: @offer %>
</div>

<div data-offer-info-id="<%= @offer.id %>" class="padded-bottom-xs">
  <%= render 'offers/status_info', offer: @offer %>
</div>

<%= render 'form' %>

<div class="text-right padded-top-xs">
  <p><%= link_to "Back to offer", offer_path(@offer) %></p>
</div>

<div class="form-separation padded-top-s padded-bottom-s">
  <div></div>
  <p><strong>Offer removal</strong></p>
  <div></div>
</div>

<div class="padded-bottom-xs">
  <p>Removing an offer is irreversible. If you want to offer services on the same topic, you will need to publish a new offer (meaning a fresh start for your reputation and your stats). Alternatively, remind that you can switch offer status to 'inactive' and reactivate it later.</p>
  <p>Please note that you can't remove this offer if you still have open deals with it.</p>
</div>

<div class="text-center">
  <%= link_to "Remove this offer", offer_path(@offer, offer:{status: :archived}), data: { confirm: "Removing an offer is irreversible. Are you sure you want to do it?" }, method: :patch, class: "btn btn-form btn-red" %>
</div>
