<% if user %>
  <% avatar_url = user.facebook_picture_url || "avatar.png" %>

  <% if user.photo.present? %>
    <%= cl_image_tag user.photo.path, width: 100, height: 100, crop: :thumb, gravity: :face, class: "#{size} bg-light-gray" %>
  <% else %>
    <%= image_tag avatar_url, class: "#{size} bg-light-gray" %>
  <% end %>

<% else %>
  <%= image_tag "account_cancelled.png", class: "#{size} bg-light-gray" %>
<% end %>
